#pragma once

// Copia este archivo a Secrets.h (mismo directorio) y completa tus datos.
// Secrets.h esta en .gitignore: NUNCA lo subas al repo.

#define WIFI_SSID       "TU_RED_WIFI"
#define WIFI_PASSWORD   "TU_PASSWORD_WIFI"

// URL HTTPS de tu servidor backend (ver carpeta server/).
#define BACKEND_URL     "https://tu-servidor.example.com/command"

// Token compartido para que solo tu dispositivo pueda usar tu backend.
// Debe coincidir con DEVICE_TOKEN en server/.env
#define DEVICE_TOKEN    "cambia-esto-por-algo-largo-y-random"
